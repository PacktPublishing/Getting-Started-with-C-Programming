#include <iostream>
#include <String>

int main()
{
	std::string grade = "";
	int testScore = 80;
	
	if (testScore >= 90)
	{
		grade = "A";
	}
	else if (testScore >= 80)
	{
		grade = "B";
	}
	else if (testScore >= 70)
	{
		grade = "C";
	}
	else if (testScore >= 60)
	{
		grade = "D";
	}
	else
	{
		grade = "F";
	}
	
	std::cout << grade << std::endl;
	std::cin.ignore();
	return 0;
}